Place real MDX .onnx models here and declare them in ../models.json.
This project ships with a builtin 'mdx-lite' (no-onnx) fallback so you can run immediately.
Replace entries in models.json with your ONNX models for high-accuracy separation.
